#:kivy 1.0
#:import kivy kivy
#:import win kivy.core.window

# P1 time left P2 time left   |
# P1 name      P2 name        | <--- in move colour, underline for "to move"
# captured pairs              |
#							  |
#settings link                |           <BOARD>
#							  |
#optional confirm button      |

# Top level

BoxLayout:
#FloatLayout:
    canvas:
        #Color:
        #    rgb: .1, 1, 1
        Rectangle:
            #source: 'images/board.png'
            source: 'images/41-old-wooden-board.jpg'
            size: self.size
        #Rectangle:
            #pos: self.center_x - 5, root.
            #size: 5, self.height

#<PanelInfo>:
	#GridLayout:
		#cols: 2
		# Left panel - info
		#GridLayout:
			#rows: 3

			# Player info
#			GridLayout:
#				rows: 2
#				cols: 2
#				Label:
#					text: "00:00"
#					# size_hint_y: 1
#				Label:
#					text: "00:00"
#					# size_hint_y: 1
#				Label
#					text: "Player1"
#					# size_hint_y: 1
#				Label
#					text: "Player2"
#					# size_hint_y: 1
#				# TODO: captured pieces

<Piece>:
	Image:
		id: image
		source: root.source
		center: 0, 0

<Pente>:
	# each time a picture is created, the image can delay the loading
	# as soon as the image is loaded, ensure that the center is changed
	# to the center of the screen.
	on_size: self.center = win.Window.center

	Image:
		id: image
		source: root.source

		# create initial image to be x pixels width
		size: 615, 615 / self.image_ratio
		pos_hint: {'right': 1}

	#Label:
		#text: "Hello world"
		# pos_hint: {'right': 1}
		# Color: rgb: .5, 0, 0

